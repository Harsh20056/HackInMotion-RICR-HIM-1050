# Samadhan - Civic Engagement Platform Environment Configuration
#
# Phase 2: auth and issue-reporting now talk to the real backend (see
# /backend). Everything else (profile, documents, AI features) still runs
# on the local mock data layer (src/shared/mock/) — that backend surface
# doesn't exist yet.

VITE_API_BASE_URL=http://localhost:4000
