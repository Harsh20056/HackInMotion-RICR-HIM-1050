# Samadhan - Civic Engagement Platform Environment Configuration
#
# Phase 1: the Supabase backend has been removed. The app runs fully
# locally with a temporary mock data layer (src/shared/mock/) and needs
# no environment variables or credentials to run.
#
# Phase 2 will introduce a new backend and its own environment variables
# here.
